# 浄めばやし — 制作記録

作った順に、踏んだ穴と直し方を残す。仕様は `SPEC.md`、シリーズ共通は `kitan-works/docs/`。

## 2026-09-01 骨を生やした（kitan-scaffold）

- 雛形の決めごと（案内カードの時間割・safe-area・束保存・送る守り・build の安全弁）はそのまま。外したものがあればここに理由を書く

## 2026-09-01 第1便（札絵58枚・遊びの芯・実測）

### 札絵は「組み立てたパス」だと artifact で消える

`build-dist.js` は **HTML／JS の中に文字列として書かれた素材の道**を正規表現で拾って base64 に差し替える。
札絵の道を `"assets/art/fuda/" + id + "_" + st + ".webp"` のように**組み立てて**書くと、正規表現に当たらない。
index.html（Pages）は相対パスのまま動くので気づかず、**artifact だけ札が全部出ない**という壊れ方をする。

→ 直し方: **58枚ぶんの道を `FUDA` という定数の表に、文字列のまま並べて置く。**参照は `FUDA[id+"_"+st]`。
表は `scripts/data/spirits-table.json` から機械で起こす（手で書かない）。
あわせて `build-dist.js` の正規表現が `assets/art/` の**1階層下まで**しか見ていなかったので、
`(?:[\w.-]+\/)*` を足して `assets/art/fuda/xxx.webp` を拾えるようにした。差し替えが58件あることを毎回数える。

### 骨の数字は、遊びを替えたら必ず矛盾する

雛形（kitan-scaffold）は「段を積む」型の数字が焼いてあった。遊びを落下タイルに替えたので、
**放っておくと静かに壊れる**箇所が3つあった。番付そのものは別便だが、数字だけは合わせた。

- `MAX_FLOOR = 80`: 束から読む値の上限。**81枚以上の記録が読み込み時に捨てられる。**→ `MAX_KEEP = 99999`（枚数は青天井）
- `SCORE_DIGIT = 100`（合成値 段×100+従）: この作の点は **枚数そのもの**（SPEC §7-a）。番付の札の表示も「n段（m）」のままだった → 「n枚」
- `TITLES` の閾値が段位（0/8/24/48/80）のまま → 枚数の目盛りへ。**文言は仮**（意匠・文章の便で決め直す）
- board 名も `main` → `main_v1`（SPEC §7-a）。**まだ一度も送っていないので、いま替えるのが唯一ただの時**

### VMAX は「満願を通す」テストでは決まらなかった

受け入れ基準は「自動プレイが108枚を通せること。`VMAX` はこの実測から決める」だったが、
測ってみると **108枚時点の速度は 277px/秒**で、`VMAX=520` には**まったく届いていない**（届くのは214枚＝延長）。
つまり満願の測定は `VMAX` について何も語らない。
→ 延長まで通す口（`#autotest=260`）を足して別に測った: **260枚・164.7秒・例外0で自動プレイは通る。**
残るのは「人が 0.35秒/行に追えるか」だけで、これは実機の宿題（SPEC §11-4）。
**「基準に書いてあるから決まる」とはかぎらない。測って初めて、その基準では決まらないと分かることがある。**

### 実測（自動プレイ・playtest ①、2回とも同値）

- 満願（108枚）まで **96.1秒**（受け入れ基準の 60〜120秒 に入る）。企画の想定「約80秒」は外れた
- 29柱すべてが1回の満願の中で灯る（`pillars: 29`）
- 札絵 58枚 = 952KB（平均16.4KB）／`index.html` 35KB ／ `dist/artifact.html` 1.33MB

### 同じ柱が盤に二枚出る（画面を見て初めて分かった）

自動プレイの実画面を撮ったら、**同じ極彩の札が盤に3枚**並んでいた。検査は全部通っていた
（例外0・満願まで通る）。**「例外が出ない」は「絵が壊れていない」ではない。**

原因は2つあって、1つ直しただけでは消えなかった。**目で見て直し、機械で数えて、まだ11件残っていた。**

1. **色の札の抽選**が「浄めた柱」から素直に引いていた。序盤は浄めた柱が数柱しかないのに、
   盤には最大6行＝12枚が出ている。**引ける数より出ている数のほうが多い**ので必ず重なる。
   → 盤に出ている柱を除く。除いて空になったら**29柱まで広げる**（「色の札は浄めた柱」は遊ぶ側に
   見えない決めごと、重なりは見える。**見える側を優先する**）
2. それでも 496回中11回 残った。**闇の札の抽選**が29柱の山から順に配るだけで、盤を見ていなかった。
   一巡の終わりぎわは山の残りが数柱で、その全部が盤に出ていることがある。
   → 闇も盤に出ている柱を飛ばす。飛ばせなくなったら**次の一巡を前倒しで足す**。
   → 496回中 **0件**。

**この手の穴は「絵が出ているか」を見るテストでは捕まらない。**盤の中身（どの柱が同時に出ているか）を
通しで覗いて数える検査を `playtest.js` に足した（200msごとに約500回）。**目で見つけた欠けは、機械で数える形にして残す。**

### この便で入れていないもの（宿題）

音（§5）／満願の見せ場・結果札・図鑑の作り込み（§4）／束に `clears`・`spirits` を持たせる（§7）／
`VMAX` の実機判断／OGP・README の本文。

**意匠の便へ申し送り**（この便では触っていない）:

- **canvas が上下に letterbox する。** `fitCanvas` は 480×720 を縦横同率で縮めるので、390×844 の端末では
  盤の上下に 76px ずつ余る。SPEC §1 は「論理480×720 を**最小に高さは伸びる**」と書いているので、
  **仕様と実装がここだけ食い違っている**。骨（kitan-scaffold）の `fitCanvas` をそのまま使った結果なので、
  外すなら意匠の便で。伸ばしても満願の秒数は変わらない（速度と行高だけで決まる）が、先読みできる行数は増える。
- 「満　天」「満　願」の浮き文字が、極彩の札の上に出るので読みにくい。地を敷くか、出す場所を選ぶか。

---

## 2026-09-02 意匠の第一段（印・盤の高さ・浮き文字）

**この段は見張り役（主のセッション）が自分で手を入れた。**意匠の担当を2度立てたが、
どちらも通信エラーで**1行もコミットせずに落ちた**。範囲を4件→3件に狭めて立て直しても同じだったので、
「誰も見ていない時間に進む」という委譲の目的が果たされていないと判断して切り替えた。
**規則（実装は担当に投げる）から外れた判断なので、理由をここに残す。**

### 1. 闇の札の印が実機寸法で見えていなかった（不具合）

- 症状: 390×844・DPR2 の実写真で、盤に出ている**3枚の闇の札のうち2枚しか枠が判別できない**。
  2倍に拡大して初めて3枚とも出ていると分かった。
- **コードは正しかった**（`!r.cleared` で必ず描いている）。**正しく描かれているのに、見えていない。**
  `2px`・不透明度 `.55` が、実寸では 1.6 CSS px の薄い線にしかならなかった。
- 直し: 金泥 `#D9A94C` の **6px** ＋ 内側に宵闇 `#131320` の **2px**。
  内側の暗い線は**絵の明るい柱でも金の縁が溶けない**ようにするため——
  **特定の暗い柱に手当てをせず、印そのものを全札に効く形で直す**（第4弾の教訓）。
- あわせて元の `rgba(212,175,55,.55)`（＝`#D4AF37`）は **`data/design-tokens.json` に無い値**だった。
  仕様を書いた側の誤り。**知っている16進ほど、系を確かめずに手が出る。**

**学び: 「拡大したら見えた」は「見えている」ではない。**
実機寸法の写真で判断がつかなかった時点で、それが既に「見えない」という答えだった。
**拡大は原因を追うときだけに使う。**検分の拡大率を上げると、見えない不具合を自分の目で塗り潰す。

### 2. 盤の高さは伸ばさない（仕様の側を直した）

第1便の申し送り「`fitCanvas` が letterbox する・SPEC §1 の『高さは伸びる』と食い違う」への裁定。
**伸ばさない方を正とし、SPEC §1 を直した。**理由は**番付の公平さ**——
高さが端末で変わると、**背の高い端末ほど先の行が見えて易しくなる**。
全国の番付で枚数を競う作なので、盤は誰の端末でも同じでなければならない。

**食い違いは「実装を仕様に合わせる」とはかぎらない。**仕様の方が浅いこともある。

### 3. 浮き文字に縁取りを敷いた

「満　願」「満　天」が単色 `#F0CE7E` で、段10 の極彩色の札に乗ると沈んでいた。
宵闇 `#131320` の縁取り（8px・`lineJoin:round`）を先に敷いてから金を乗せる形にした。

---

## 2026-09-02 第3便（束の clears・spirits／結果札／図鑑29柱）

### 1. 束に入れ子を持たせるときは「知っている鍵」だけ読む

`spirits`（柱ごとの浄めた数）は束で初めての**入れ子**。`best` のような数値と違い、
**壊れた束・他所の束が来たときに、鍵の数だけ膨らむ**。
→ `mergeSave` は `SPIRITS` の29柱を順に見て、その鍵だけを読む（`Object.keys(o.spirits)` で回さない）。
値は `best` と同じ物差し（0〜`MAX_KEEP` の整数）で、範囲外は**丸めずに捨てる**。

あわせて `persistSave` は `{ ...saveData }` の浅い写しでは足りない。
**入れ子は参照が渡るので、SDK が握ったまま次の夜の加算が混ざる。**`spirits` も写して渡す。

束へ足すのは**夜の終わりに1回**（`showGameOver` の中）。稽古の夜は足さない——
「送らない経路」と「残さない経路」が同じ `!practice` の1か所で決まる形を崩さない。

### 2. 「最高記録を更新」が二重になっていた

結果札に自己最高の行を足したら、番付の1行目（`#final-rank-mark`）と**同じことを二度言う**画になった。
順位の行は SDK の `improved`（番付側の記録）、新しい行は束の `best`（自分の記録）で、出どころが違う。
→ 順位の1行目を「**番付の記録を更新**」に直し、自己最高は新しい行が言う。
**SDK が読めない夜（Artifact・枠の外）でも自己最高の更新が出る**という利点もそのまま。

### 3. 29柱の図鑑は、札ごと送ると閉じられなくなる

図鑑は番付と同じ「開いて閉じる札」（SHITSURAE §7）にしたが、**番付は10行、図鑑は8行×4列**。
`.bz-card` の `overflow-y: auto` をそのまま使うと、**下まで送ったとき ✕ が画面の上へ流れて消える。**
札が画面をほぼ埋めるので**外側タップの余地も無く、閉じる手が両方とも失われる。**
→ 送るのは升目（`#zk-grid`）だけにして、札自身は `overflow: hidden`。✕・見出し・「n／29 柱」は残る。

**同じ型を写すときは、型が前提にしていた量を確かめる。**この穴は行数が増えて初めて出る。
検査（playtest ①）に「下まで送っても ✕ が見える」を数える形で残した。

### 実写真で確かめた寸法（DPR2）

| 画面 | 結果札 | 「もう一夜」の下端 | 図鑑の札 |
|---|---|---|---|
| 390×844 | 463px | 625px | — |
| **375×667**（天井の検分） | 381px | **509px**（画面667） | 643px（上限655）・名前12px |

順位2行＋図鑑の入口を足しても天井は割っていない。図鑑は下まで送っても ✕ が天 23px に残る。

### この便で入れていないもの（宿題）

音（§5）／満願の一斉染めの演出／OGP・README 本文／`VMAX` の実機判断。
図鑑の入口は**結果札だけ**（SPEC §4 のリザルトに置いた）。題字の札からは開けない——
戻ってきた人が遊ばずに集めた柱を見に行けないので、入口を増やすかは意匠・企画の裁定を待つ。

## 2026-09-02 音の実装先が生成物だった（検査が緑のまま壊れていた）

音の担当は `index.html` に83行を書いてコミットした。**`index.html` は `scripts/build-dist.js` が
`src/page.html` と `src/game.js` から作る生成物**なので、**次に誰かがビルドした瞬間に83行すべてが消える。**

- `src/game.js`・`src/page.html` の `kotoStep` / `noteFreq` / `scaleFreq` の出現数は **0**、`index.html` だけ 10 だった。
- **`playtest.js` は `index.html` を見る。**だから生成物だけに書かれたコードでも**8件すべて緑のまま通る。**
  担当の報告（波形の実測・自己相関で±30セント以内・実クリックで `kotoStep` が進む）も**全部本当だった。**
  測ったものは正しく、**測る場所が生成物だった。**
- 直し: 差分から81行を取り出して `src/game.js` へ移し、ビルドし直して `index.html` の差が
  **空行1つだけ**であることを確かめた（＝写しは忠実）。測り直しても階段は同じ、全音が200Hzの床超え。

**学び: 「どこを直したか」は「何を直したか」と同じくらい効く。**
生成物に書いた実装は、動くし、検査も通るし、報告も正しい。**次のビルドまでは。**

→ **`kitan-precheck` に検査を1項目足した**（2026-09-02）: 「生成物だけに書かれたコードが無い」＝
`build-dist.js` を実際に走らせて `index.html` を作り直し、前後をバイトで比べる。変われば `src` に無い実装がある。

## 2026-09-02 第2便（夜明け・満願の音／音量の釣り合い・音響担当）

実装・実測は SPEC §5「夜明け・満願の音／音量の釣り合い」に書いた。ここには工程で踏んだ地雷だけ残す。

- **公式SE9本のうち `whiff`（夜明け）・`end`（満願）だけ残し、残り7本は `assets/audio/` から削除した。**
  リポジトリに置いてよいのは実際に鳴らす音だけ、という規約どおり
- **Artifact版で `fetch("assets/audio/*.m4a")` をそのまま使うと、build-dist.js の既存正規表現
  （`assets/(?:audio|art|fuda)/...` を丸ごと data:URI に置換）が fetch の引数の文字列まで書き換えて
  しまい、Artifact の CSP で `fetch("data:")` が通らず無音になる**（宵あらわし・御霊そろえで先に
  踏まれていた地雷と同じ形。今回は実装前に他作のコメントを読んで先回りできた）。
  直し方: 画像はそのまま既存の正規表現（`assets/(?:art|fuda)/...`）で data:URI 化、音声だけは
  `src/game.js` の `SE_NAMES` を `build-dist.js` が読み取って `window.AUDIO_DATA` にbase64を注ぎ、
  `game.js` 側は `window.AUDIO_DATA` があれば `atob()` → `decodeAudioData`、無ければ
  `fetch("assets/audio/...")` にフォールバックする（宵あらわしの `b64ToBuf` と同じ形）
- **ローカルの検証だけでは「動く」ことしか言えず、「Artifactで動く」ことは別に確かめた。**
  puppeteerでネットワークの `assets/audio/*` へのリクエストを遮断し、`window.AUDIO_DATA` だけで
  デコードが通ることを確認。さらに `dist/artifact.html` を実際にビルドして同じ遮断状態で開き、
  `build-dist.js` が実際に埋めた `window.AUDIO_DATA` からもデコードが通ることまで確認した
  （＝「index.htmlでは動くがdist/artifact.htmlでは無音」という、前便と同じ根の事故を先に潰した）
- **音量は最初の目安（whiff のゲイン0.5）で仮組みし、実測（-24.4dB）が夜明けの音としては沈みすぎたので
  0.7へ上げ直した。**紙の計算（ffmpegでの粗い推定）とブラウザの実デコード後の値はズレる
  （AACのデコーダが違う・サンプルレートも44.1kHzから48kHzへ変わる）ので、**最後は必ずブラウザで
  実測してから決める**
- **`purify()`/`showGameOver()` から実際に呼ばれているか**は、`window.__se` に呼び出し回数
  （`counts`）を足して検証した。実クリックで108枚めに `end` が1回、わざと外した瞬間に `whiff` が
  1回だけ鳴ることを確認（前便の「生成物に書いた」事故のあと、「呼ばれる場所」まで含めて測る習慣にした）

---

## 2026-09-02 セッションの引き継ぎ（ここまでの到達点）

**入っているもの**: 遊びの本体（四筋・一対の並び・判定・速度・百八枚の満願）／札絵58枚（29柱 × 段03・段10）／
闇の札の印／図鑑29柱／結果札／束（`best`・`clears`・`spirits`）／全国番付の配線／
琴の音（都節音階・Karplus-Strong）／夜明け(`whiff`)・満願(`end`)の音。

**検査**: `playtest.js` **8件すべて緑**（満願96.1秒・盤の重なり0・図鑑の閉じ手・全ルート送信0回）。
`kitan-precheck` **✓29 ✗1 △2**（✗ は `ogp.png` 未作成の1件だけ）。

**途中で切れた作業**: **満願の見せ場（盤の一斉染めと月・SPEC §4）を担当に出した状態でセッションが終わった。**
コミットは `2e92ae6` までしか無いので、**この作業は残っていない**。次のセッションで出し直すこと。

### 次に手を付けるもの（順）

1. **満願の見せ場**（SPEC §4）。**満願は夜の終わりではない**ので、拍を切らないよう1秒前後に収める
2. **OGP**（`scripts/ogp-card.html` を headless Chrome で撮る。`kitan-precheck` の唯一の ✗ がこれ）
3. 公開一式（`docs/RELEASE.md`: GitHub → Pages → OGP → わいわい掲載 D0 → X告知 D+2）

### オーナーの裁定を待っているもの（2件・どちらも機械では出ない）

- **人が追える速さか。**満願まで96.1秒、後半は0.65秒/行。`VMAX=520`（0.35秒/行）は延長でしか効かない。
  実機で遊んでもらうまで決まらない（`dist/artifact.html` は出来ている）
- **音の折り返し**（SPEC §5「折り返しの決めどころ」）。いまは220→932Hzの12音のあと**上の5音を反復**。
  一夜108枚のうち別々の音は最初の12枚だけ。対案は**220へ戻る鋸歯状**。
  **題名『浄めばやし』は「叩き続けると旋律になる」を芯にした名前**なので、ここが薄いと改題（『闇はらい』）の話になる

### 実機確認用 Artifact（2026-09-02・未公開のまま）

オーナーが実機で裁定2件（速さ・音の折り返し）を出すために、`dist/artifact.html`（コミット `541236c` 時点のビルド）を
Claude Artifact に出した: https://claude.ai/code/artifact/ba79a383-adbe-4c1f-94c0-976ac0f84d38
枠内では `waiwai.town/sdk.js` が CSP で読めないので番付は出ず、記録は端末内だけ（`waiwaiTry` が2.5秒で諦める設計どおり）。
直したら同じ URL に更新する（別の Artifact を増やさない）。

### オーナー実機の裁定（2026-09-02・Artifact で 333枚まで）

- **速さ: 追える。**333枚（延長・`VMAX` の領域）まで到達。「ここまで来ると難しい」「最初はちょっと単調だが、それくらいでもいい」→ 速度曲線は現状維持
- **音の折り返し: 現状は不可。**「高速になると5音の繰り返しは単調」。提案は「月蝕綺譚の曲を琴の旋律で再現し、80〜90枚あたりから『あの曲だ』と分かる」形。権利（ガイドライン）と曲の在庫を調べてから SPEC §5 を書き直す
- **「月蝕綺譚の曲を琴で」の実現性（09-02）**: 権利OK（公式「曲のお約束」にアレンジ・カバーOKの明記）。機械採譜は下書き止まり（`scripts/melody/README.md`）。
  耳で判定するための試聴ページ『囃子帖』: https://claude.ai/code/artifact/a7779908-e9c6-49cc-9686-e4b0eeed8e18
- **旋律の裁定（09-02）**: 三系統のうち**当方の機械採譜（`scripts/melody/phrases.json` の Aメロ／Bメロ／サビ1〜3）を採用**。「音の上下があって遊んでいて良さそう」が理由（正確さでは選んでいない）。BGM の要否は検討中（SPEC §5 の「BGMは入れない」は、囃子帖の「宵の底」を聴いて決める）

### 序盤の床（2026-09-02・オーナー裁定「持続音は無し。その代わり最初をもう少し速く」）

- BGM／拍のない持続音は**入れない**で確定（囃子帖で聴いたうえでの裁定）。序盤の間は**速度**で埋める
- `VFLOOR=180`（1.0秒/行）を敷き、助走は床×0.85。**曲線 `V0×ACC^n` は触っていない**ので37枚め以降は旧どおり
  （曲線全体を上げると、オーナーが「追える」と裁定した後半まで動くため）
- 実測: 満願 92.4秒（旧96.1）・`playtest.js` 8件緑。`dist/artifact.html` を作り直して同じ Artifact に更新した
- `playtest.js` は `puppeteer-core` を `NODE_PATH=../shikifuda-kasane/node_modules` で借りて動かす（このリポジトリには node_modules を置いていない）

### 本歌（2026-09-02・オーナー裁定「最初から採譜したものに」）

- 当方の機械採譜（`scripts/melody/phrases.json` の Aメロ→Bメロ→サビ1→サビ2→サビ3）を `MELODY`（MIDI 164音）として `src/game.js` に置いた。
  1タップ＝1音、夜ごとに頭から、164音で一巡して繰り返す。都節音階の上行と5音反復は撤去（SPEC §5 に来歴と設計）
- 偶然の一致: 序盤1.0秒/音≒Aメロの自然な速さ、満願前後0.65秒/音≒サビの速さ。称号 72枚＝サビ1の頭、108枚＝サビ1の終わり
- `playtest.js` 8件緑・`dist/artifact.html` を同じ Artifact に更新
- **未決**: 108（満願）を節目として残すか、純エンドレスにするか（オーナーが「ゴールのないゲームでも良いかも」と発言）。
  残すなら見せ場は1秒・盤を止めない（§4）。無くすなら見せ場の実装が不要になる

### 段が上がる夜（2026-09-02・オーナー指摘「鐘ひとつでは節目が弱い」）

- 企画は `kitan-circle/data/articles/20260902-1020-7sakume-fushime-plan.json`、決めごとは SPEC §4「段が上がる夜」。
  段06〜09 を `fetch-fuda.py --ranks 6-9` で取り、240×360 webp に縮めて `assets/art/fuda/` へ（174枚・2.7MB。原寸は `.gitignore` の `assets/fuda/`）
- 実装: `STAGES`／`stageOf`／`colorRank()`、節目の一斉染め `sweep`（金の線が0.6秒で下から上へ・線より上は前の段）、`drawGround()` の段階描画。
  `FUDA` の表は174件を**文字列で並べる**（build-dist の正規表現の都合。組み立てた道は拾われない）
- **検査の地雷**: `playtest.js` ⑤ を「setInterval で evaluate ＋ その中で screenshot」の形で書いたら、段08のあとで
  `Runtime.callFunctionOn timed out`（180秒）で落ちた。**ゲームは無事だった**（別の直列プローブで470枚・5段・例外0を確認）。
  puppeteer は同じページへの evaluate と screenshot を重ねると詰まる。**覗きとスクショは直列の while ループで回す**。
  ついでに `ONLY=5` で1本だけ回せるようにした（5本通しは6分かかる）

### 一斉染めの撤去と、本歌の差し替え（2026-09-02・オーナー実機）

- **一斉染めは撤去**（「ミスしそうになった」）。行は生まれた時の段を持ち、節目でも盤の上の札は動かない。鐘と浮き文字だけ。playtest 9件緑・Artifact 更新
- **旋律はメインテーマの採譜をやめ、版権切れの名曲へ**（オーナー裁定「誰もが知っていて版権も切れた曲。譜はネットにある」）。
  候補7曲を譜のある出どころ（Wikipedia の `<score>`＝LilyPond／Commons の MIDI／Commons の楽譜画像）から機械で読み、
  囃子帖（Artifact）に載せ替えた。一覧と推しは `kitan-circle/data/articles/20260902-1920-7sakume-honka-candidates.json`。
  道具は `scripts/melody/ly2notes.py`（LilyPond→MIDI番号）。**ゲーム本体の `MELODY` はまだメインテーマの採譜のまま**＝曲が決まったら差し替える

### 曲が替わる夜（2026-09-02 夕・オーナー指摘「108と決めず曲の終わりで。演出は曲に合わせる」）

- `STAGES`（108の倍数）を `SONGS`（曲ごとに 段・空・最低枚数・譜）に置き換えた。切り替えは**曲が一巡し終えた時だけ**（`songStep` が曲の長さに達した時、`purified >= 次.min` なら進む）
- 空の演出 `drawSky()`: 荒城の月＝影の円をずらして三日月→満月／朧月夜＝靄の帯／さくらさくら＝夜桜（#8E6B9E）／山の魔王＝月が朱へ（#F0CE7E→#E0562F）。汎用の飾り（菱・蝕環・金粉・金雲）は撤去
- 段07の札絵29枚を削除（使わない絵は置かない）。145枚・約2.3MB
- 朧月夜の譜は ja.wikipedia「朧月夜 (歌曲)」の `<score>`（`\transpose c d` のハ長調表記→+2）
- **月の置き場は実画面で決めた**: 最初は画面上部（y=150・r=84）に置いたが、札は各行2枚で上部の行に隠れ、24枚・140枚の実画面で月がほぼ見えなかった。
  **盤の真ん中に大きく（y=270・r=132）**へ。どの行でも札の隙間から一部が見える。朧月夜は本体を薄く（α.32）暈を広く（r×2.0・α.22）、靄の帯を濃く（α.22・高さ68）した
- 検査: ①〜⑤ 9件緑。⑤の切り替わりは 96（荒城の月×2）→218（朧月夜×2）→318（さくらさくら×2）→山の魔王（以後）

### 空を絵にする（2026-09-02 夜・オーナー指摘「月などのイラストがしょぼい。Lovart で。額装や背景も札絵の品質に」）

- Lovart（GPT Image 2・プロジェクト 56e1b22b…・スレッド f98cae9c…）で8枚: 満月（黒地・`lighter` 合成用）／背景6（荒城・朧の菜の花・夜桜・月光の湖・浜辺の明け）／額装（黒地に金蒔絵の枠）。
  血月は `mitama-otoshi/assets/art/eclipse.jpg`（Lovart 生成済み）の上部を流用。プロンプトの型は御霊おとしと同じ（「1枚だけ・展開不要」「中央は低コントラスト（札が並ぶ）」「月は描かない（別層）」「黒地（合成で透明にする）」）
- 変換 `scripts/sky/convert.py`（480×720 cover 上寄せ／月 512²／黒を締める LUT）。**同じスレッドの download は前の生成物も落とす**ので「前の工程に無かったファイル」を採る
- 合成: 背景（.9・曲の一巡目で前の曲から溶け替わる）→ 月（`lighter`・不透明度＝曲ごとの規則: 荒城 p／朧 .65／魔王 1−p／月光 p／浜辺 1−.7p）→ 靄・花弁 → 額装（`lighter` .9）→ 筋の境 → 札。図形の月・蝕環・金雲は撤去
- 5・6曲目: 月光（+12・還る月）・浜辺の歌（明け）。最低枚数 430／560。⑤は 620枚通しに
- 重さ: 空8枚で 171KB（暗い絵は軽い）。生成物 3.46MB
- **空の静止部分は裏の canvas に焼く**（`bakeSky`）: 背景の溶け替わり・月・額装は1タップごとにしか変わらない。毎フレームは焼いた1枚＋靄＋花弁だけ＝`lighter` の全面合成を毎フレームやらない（スマホの負荷）
- **検査の地雷（2回目）**: 空の絵を入れたあと `page.screenshot` の直後に `Attempted to use detached Frame` で落ちた（ゲームは直列プローブで620枚無事）。⑤の撮影は `canvas.toDataURL` の直取りに変更。**puppeteer の screenshot は重い盤では使わない**

### 稽古・帳・案内の絵・記録の2軸・結果札の絵（2026-09-02 夜・オーナー指摘「テストモード、飛びの開幕、番付など他作の要素を」）

- 稽古は「外しても取りこぼしても終わらない・本番の速さ・記録なし」に改訂し、案内の札に見える釦を足した（題字3タップも残す）。検査③は 20枚で `__forceOver` して数える
- 帳は月影とびの CSS をそのまま写し、声の 350ms を抜いて 500ms で開く。自動プレイは `begin(false, true)` で飛ばす
- 案内の絵は Lovart（同じスレッド）で1枚。「満月の下に四筋の札・下1/3は暗く」の指定どおり
- 記録に `songMax`（届いた曲）を足し、案内の誉れ行に「〜まで」。結果札にその夜いちばん浄めた柱の絵
- 番付は既に §7 の型で入っている（変更なし）。Artifact では SDK が読めないので見えない
- 2026-09-02 深夜: 「満天」の浮き文字を撤去（効果が薄い）、筋の境の1px線を撤去（満月を縦に横切って見えた）。オーナー実機の指摘。Artifact 更新
- 同夜: 目付（qa）と金継ぎ（designer）のフルチェックを並行で依頼。結果は `docs/ut/`（2026-09-02）と `kitan-circle/data/kintsugi/2026-09-02-kiyome-bayashi/`
- **金継ぎ（2026-09-02・意匠担当）**: 6画面×2寸で破れ0件（コントラスト・12px床・押し所24px・角丸4/5/10・禁止色・二色一組の地）。任意の底上げ2点を本体へ写した: 番付「あなた」行の順位数字を本文色へ（比4.72→約11）、主要な釦に `:active{translateY(1px)}`（tap-highlight を消しているぶんの代わり）。
  `--line: #4a4468` がトークンの系に無い件は次のトークン更新へ送る。診断書 `kitan-circle/data/kintsugi/2026-09-02-kiyome-bayashi/SHINDAN-20260902.md`。**自分で検算**: 「あなた」行の地は `.bz-row.me` の background が置き換え（重ならない）なので 金泥.12 over #1B1B2E ≒ (50,44,50)、#9D93B5 で比4.7＝報告どおり
- **目付（2026-09-02・検品担当）**: 15シナリオを機械で実施（`docs/ut/` 2026-09-02-q8h3）。要修正3件を直した: ①満願（108）に印が無い→浮き文字「満　願」（鐘は曲の節目のまま）②README の番付の節が雛形のまま（段×100・board main）→枚数そのもの・`main_v1`・遊び方も実文へ ③入力解禁1.5秒では帳が左右26px残る→2.1秒。実機待ち: 音・速さの体感・枠内の本物の番付・マナーモード
- **108（満願）の撤去（2026-09-03・オーナー「108にまたこだわっている。資料にゴミが残っているのでは」）**: 称号（108/216/324/432）・結果札「明けの祓い」・案内の「百八枚で満願」・束の `clears`・受け入れ基準2・README・SE のコメントから外した。
  称号は最初の曲の間だけ枚数、2曲目からは曲名（`titleFor(n, stage)`）。検査①②は「2曲目に入るまで（96枚）60〜120秒」に。SPEC の制作記録（旧）の節には履歴として残る
- **延長の緩い傾き（2026-09-03・オーナー裁定「ゆるい傾きで速くしたい」）**: 第一の上限（520・214枚）に届いたあと `ACC2=1.0008` で上げ続け、第二の上限 `VMAX2=720`（0.25秒/行）に621枚で届く。440枚で0.29秒/行。214枚までの曲線は不変
- 検査⑤の撮影付き（`SHOTS=`）は3回に1回ほど `detached Frame` で落ちる（Chrome の描画側の不安定・ゲームは直列プローブで無事）。**門として回すときは撮影なし**（266.5秒・緑）、写真が要るときだけ `SHOTS=` を付けて別に回す
- **称号の復活・稽古の釦の撤去・ボイス2本（2026-09-03・オーナー裁定）**: 称号は曲ごと（聞き役→朧払い→花吹雪→紅蝕→月還り→渚の唄い手・`SONGS[].title`）。稽古の見える釦は撤去し題字3タップだけに。ボイスは弁天の開幕・夜明けの2本（`SE_NAMES` に `koe_start`／`koe_dawn`、`SE_VOL` 1.0、稽古では夜明けの声を鳴らさない）。台詞は `kitan-circle/data/articles/20260903-0930-kiyome-titles-voice.json`
- **ボイス2本の検算（2026-09-03・自分で測った）**: 尺 1.97秒／1.51秒（上限内）、頭の無音 0.04秒／0.00秒、末尾の無音 0.09秒（切れなし・末尾0.15秒の平均はピークの2〜4%）、0.1秒窓RMSの最大 -9.4dB／-13.2dB＝琴（-18.1dB）・whiff（-18.3dB）より前に出ている（MEDIA.md「声 > 効果音 > BGM」どおり）。
  両経路（index.html の fetch／dist の `AUDIO_DATA`・assets遮断）で4本すべてデコード成功（`__se.loaded` が true）。`__se.loaded` は**その夜に鳴っていない音があると false のまま**なので、検証では4本を明示的に鳴らしてから見る
- **開幕の声を差し替え（2026-09-03）**: 候補13本からオーナーが「ぬしさん、唄に付き合いなんし」（間の圧縮なし・3.11秒）を選定。入力解禁を3.45秒へ、`SE_VOL.koe_start` を0.36へ（素で -9.0dB と出過ぎ）。**尺に合わせて声を詰めるのではなく、尺のほうを声に合わせた**
- **曲の節目のボイス5本（2026-09-04）**: 弁天。朧月夜「月に、靄の帯がかかりんす」／さくらさくら「花びらが、舞い込みんす」／山の魔王「月も、蝕まれてゆきんす」／月光「月が、還ってきなんした」／浜辺の歌「波間に、明けの色がさしんす」。
  鐘（`end`）の0.7秒後に鳴る（`SONGS[].voice`）。全7本の実測は `docs/VOICE.md`。**`koe_start` の音量を 0.36→0.62 に訂正**（-17.9dB に沈んでいた）。ボイス帖 `kitan-circle/data/voice-kiyome/index.html`
- **更新漏れ（2026-09-04）**: 節目のボイス5本を入れてビルドしたが Artifact を再公開しておらず、オーナーの実機では鳴らなかった（「96枚で声が聞こえない・2回試した」）。dist を作り直しても公開しなければ届かない。**足したものが公開物に載っているかを、報告の前に確かめる**


---

## 2026-09-04 セッションの引き継ぎ（ここまでの到達点）

**コミット `8246cfe`「第7弾『浄めばやし』を作り直す」に一連が入っている（198ファイル）。未コミットは無い。**
**リモート未設定＝まだ push していない**（公開のときに `docs/RELEASE.md` の作法で作る）。

### 入っているもの

遊び（四筋・一対・床180px/秒→曲線→第一の上限520〈214枚〉→ `ACC2` の緩い傾き→第二の上限720〈621枚〉）／
札絵145枚（29柱 × 段03・06・08・09・10）／図鑑29柱／結果札（称号・枚数・最高・その夜いちばん浄めた柱の絵）／
束（`best`・`spirits`・`songMax`）／全国番付／**版権切れ6曲**（荒城の月→朧月夜→さくらさくら→山の魔王→月光→浜辺の歌・
1タップ1音・曲が一巡し終えた時だけ次へ）／**Lovartの空**（背景6・満月・額装・案内のキービジュアル）／
**弁天のボイス7本**（開幕・節目5・夜明け）／帳／稽古（隠し入口のみ・外しても終わらない）／称号（曲ごと）。

### 検め

`playtest.js` **9件緑**（620枚・6曲・例外0。⑤は**撮影なしで回す**＝`SHOTS=` を付けると3回に1回ほど
`detached Frame` で落ちる）。目付15シナリオ（`docs/ut/2026-09-02-q8h3`）と金継ぎ（破れ0・
`kitan-circle/data/kintsugi/2026-09-02-kiyome-bayashi/`）を通し、要修正3＋任意2を反映済み。
`kitan-precheck` は**この一連のあとまだ回していない**（OGP未作成のはずなので ✗1 の見込み）。

### 残っているもの

1. **OGP**（`scripts/ogp-card.html` を headless Chrome で撮る）
2. **公開一式**（`docs/RELEASE.md`: GitHub作成 → Pages → OGP → わいわい掲載 D0 → X告知 D+2）
3. `kitan-precheck` を回して ✗0 にする

### オーナーが実機で確かめたこと（2026-09-03〜04）

速さ（440枚まで到達・「良い難しさ」）／曲と空／稽古／開幕と夜明けの声／96枚の節目の声。
**残る未確認は 218・318・453・597枚の節目の声**（機械では5本とも1回ずつ鳴ることを確認済み）と、
血月（山の魔王の空）の是非は **2026-09-05 に「出して良い」で採用**（保留は解けた・SPEC §4）。

### この一連で踏んだ穴（次作にも効く）

- **ビルドは公開ではない。**節目の声を入れてビルドし検査も緑にしたのに Artifact を再公開せず、
  オーナーの実機では鳴らなかった。**足したものが「相手が触っている版」に載っているかを報告の前に確かめる**
- **裁定で消えた概念は資料から一括で消す。**108（満願）を節目から外したのに仕様書・称号・検査に残り、
  目付が「満願に見せ場が無い」と拾って実装が浮き文字を足した＝**古い決めごとが検査を通じて復活する**
- **声は尺に押し込めない。**間を詰めた版は処理の痕が残って不採用になった。**待ち時間のほうを声に合わせる**
- **音量の倍率は dB から逆算する**（`20·log10`）。当て推量の 0.36 で開幕の声が -17.9dB に沈んでいた
- **一枚絵を敷いたら、その上に残る図形の線を全部疑う**（筋の境の1px線が満月を縦に横切っていた）
- **叩く盤の上を横切る演出は入れない**（節目の一斉染めは「ミスしそうになった」で撤去）
- **月は上に置くと札に隠れる。**演出の置き場は「空いている所」でなく「隙間から見える大きさ」で決める

---

## 2026-09-05 公開（GitHub → Pages）

**https://hirohgxx.github.io/kiyome-bayashi/ で公開した。**リポジトリは
`hiroHGxx/kiyome-bayashi`（public・homepage 欄も埋めた）。`docs/RELEASE.md` §2 の順どおり。

- 出す前に**追跡ファイル全体を検めた**（Pages は `scripts/` も `docs/` も配信するため）。
  手元の絶対パス・APIキーの混入なし。追跡 31MB のうち 26MB は目付の写真（`docs/ut/`）
- Pages 有効化から **20秒で200**。配信された `index.html` は手元のビルドと **sha256 一致**（69,846 bytes）
- OGP: `ogp.png?v=1` が 200・`image/png`・1.36MB。**Twitterbot のUAで引いて** og:title／og:description／
  og:image／og:url／twitter:card=summary_large_image／twitter:image がすべて返ることを確認
- **iframe を妨げるヘッダーは無し**（わいわいタウンの枠で読める）
- 素材は音9・空8＋題字・札絵の抜き取りで28件すべて200。札絵は145枚すべて追跡されている
- **実物で遊べることまで見た**（Pages のURLに対して10枚浄める・例外0・盤が描かれている）。
  この検めでも わいわいタウンへの通信は遮断している（本番の番付を汚さない）

**残り: わいわいタウン掲載（D0）→ X告知（D+2・オーナーが押す）。**
掲載にはサムネ（640×640）とプレビュー動画が要る。素材は 09-05 に撮った実プレイの静止画6枚
（`scripts/pv/shots/`・撮り直しは `scripts/shots.js`）が使える。

---

## 2026-09-06 セッションの引き継ぎ（公開の途中・素材は揃った）

### いまどこまで

- **Pages 公開済み**（09-05）: https://hirohgxx.github.io/kiyome-bayashi/ ／ repo `hiroHGxx/kiyome-bayashi`（public・homepage 記入済み）。
  配信物は手元と sha256 一致・OGP 6項目・素材28件200・実物で10枚浄めるところまで確認済み
- **`kitan-precheck` ✓30 ✗0 △2**（09-05）。未コミット無し・push 済み
- **血月（山の魔王の空）は採用**（09-05 オーナー裁定「出して良い。良い感じ」）
- **掲載素材は4点そろっている**（`media/`。**.gitignore の中なので git には無い＝手元にだけある**）:
  | 物 | 寸法・尺 | 音 |
  |---|---|---|
  | `media/waiwai-thumb.png` | 640×640（弁天の段10・文字なし） | — |
  | `media/waiwai-preview.mp4` | 640×640・12.8秒・3.1MB | **無音**（掲載規格） |
  | `media/kiyome-bayashi-pv.mp4` | 720×1280・27.7秒・10.5MB | 実音（琴・鐘・弁天の声） |
  | `media/kiyome-teaser.mp4` | 720×1280・10.2秒・5.8MB | 実音（Xのチラ見せ用） |
- 実プレイの静止画6枚（曲ごと）は `scripts/pv/shots/`（同じく git の外）
- **X のチラ見せ文面は用意済み・未投稿**（`kitan-circle/data/articles/20260906-1030-kiyome-songs-x-copy.json`）

### 残っていること

1. **『荒城の月』の版の裁定（先に決める）** — 下の申し送りを読む
2. **わいわいタウン掲載（D0）** — `submit_app` ＋ サムネ・プレビュー動画。**送る文面を先にオーナーへ見せる**（掲載は取り消せない）
3. **X告知（D+2・オーナーが押す）** — 文面は writer が用意する。チラ見せを先に出す場合はその後
4. 掲載後、枠の中で1回遊んで**本物の番付に送れること**を確認する

### 申し送り: 『荒城の月』は山田耕筰版の旋律を鳴らしている（未裁定）

- README とこの仕様書は「**版権の切れた曲**」と書きつつ、`荒城の月（滝廉太郎 1901・**山田耕筰版**）」と明記している。
  **滝は1903年没＝原曲は自由。しかし山田耕筰は1965年没で、編曲の保護は日本では2035年末まで残る**
- **実装の音を数えたら、48音すべてが D・E・F・G・A・A♯ で嬰記号の音が1つも無い。**
  原曲にあって山田版で半音下げられた音が無い＝**鳴っているのは山田版の旋律**（`src/game.js` の `SONGS[0].notes`）
- 一音の違いなので編曲として保護されるかは議論の余地があるが、**「版権が切れている」と公に書きながら
  「山田耕筰版」と明記している状態**は直しておくほうが安全。直し方は2つ:
  1. **原曲に戻す**（該当の1音を半音上げ、README から「山田耕筰版」を外す）＝音を1つ変えるだけ。検査も撮り直しも不要（PVの1曲目の響きが少し変わる）
  2. **曲を差し替える**（荒城の月をやめて別の版権切れ曲に）
- **オーナー裁定待ち。**決まるまで、権利の言い方を公の文（X文面）に書かない

### 撮り直しが要るとき

素材の元（screencast のコマと音）は**この会話の作業場に置いたので消える**。撮り直しは台本が残っているので4〜5分:

    NODE_PATH=../shikifuda-kasane/node_modules node scripts/pv/capture-pv.js <出力先>   # 血月まで約200秒
    NODE_PATH=../shikifuda-kasane/node_modules node scripts/pv/capture-open.js scripts/pv/opening.png
    NODE_PATH=../shikifuda-kasane/node_modules node scripts/pv/make-cards.js
    bash scripts/pv/make-preview.sh <出力先> ; bash scripts/pv/assemble.sh <出力先> ; bash scripts/pv/make-teaser.sh <出力先>

**PVは画面の写し。画面を変えたら告記の前に撮り直す**（MEDIA.md）。

### この便で踏んだ穴（映像まわり・次作にも効く）

- **自動プレイで撮った画は宣伝に使えない。**`#autotest` は出た瞬間に叩くので**闇の札が一枚も写らない**。
  宣伝の画は人と同じ間合い（下まで落ちてから叩く・`scripts/shots.js`／`capture-pv.js`）で撮る
- **見せ場が遠い作は録画を開け閉めして要る場面だけ撮る**（血月は390枚＝約200秒。通しだと数百MB）
- **一枚絵に fade をかけるとテロップが出ない**（単一コマの alpha=0 が繰り返される）→ `-loop 1` で映像にしてから
- **webm(opus) のまま `atrim` で切ると尺がずれる**（先頭の壊れたパケットで合計1.74秒短くなった）→ PCM に落としてから `-ss/-t`
- **`xfade` の合計尺は足し算で出す**（`-t` では伸びない）
- **`alimiter` を頭打ちに使わない**（既定 `level=true` で抑えたぶんを持ち上げ直す）。素直に `volume=-2dB`
- **音の割れは AAC 化の後で測る**（組んだ wav が -0.6dB でも AAC で 0.0dB に張り付く）
